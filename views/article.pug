extends layout

block content
    h1= article.title
    h5 Escrito por 
        strong #{author}
    p= article.body
    hr
    a.btn.btn-default(href='/articles/edit/'+article._id) Editar Articulo
    a.btn.btn-danger.delete-article(href='#', data-id=article._id) Eliminar Articulo